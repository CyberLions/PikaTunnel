{
  "name": "proxies-and-tunnels",
  "private": true,
  "scripts": {
    "dev": "pnpm run --parallel /dev/",
    "dev:frontend": "cd frontend && pnpm dev",
    "dev:backend": "cd backend && uvicorn app.main:app --reload --host 0.0.0.0 --port 8000"
  }
}
